#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    while (1) {
        int num;
        cin >> num;

        if (num == 0) {
            break;
        }
        cout << num << endl;
    }
    return 0;
}